using UnityEngine;

namespace Markers
{
    // Marker class for guns to find this at runtime. I think this is probably better than using Gameobject.Find()
    // but my architecture is dogshit so i think i shouldn't be in this position in the first place.
    // allahumma anqidh roohi
    public sealed class PlayerRecoilObject : MonoBehaviour
    {
    }
}