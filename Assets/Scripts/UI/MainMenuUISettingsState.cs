using System;
using UnityEngine.UIElements;

namespace UI
{
    public class MainMenuUISettingsState : SettingsUIState
    {
        public MainMenuUISettingsState(VisualElement rootElement, Action onClose) : base(rootElement, onClose)
        {
        }
    }
}